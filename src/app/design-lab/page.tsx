import { DesignLab } from "@/components/design-lab/shell";

export const metadata = {
  title: "Design Lab — Andrei Motorbike Rent",
  robots: { index: false, follow: false },
};

export default function Page() {
  return <DesignLab />;
}
